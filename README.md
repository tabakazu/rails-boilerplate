# README

## Setup
```bash
# Clone Repository
$ git clone git@github.com:tabakazu/rails-boilerplate.git
$ cd rails-boilerplate

# Setup
$ docker-compose run --rm rails bin/setup

# Start Web Server ...
$ docker-compose up rails
```
